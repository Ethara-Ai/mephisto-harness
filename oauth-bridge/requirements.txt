# Dependencies for the Claude Code OAuth bridge (host-side proxy).
# Install into the same venv you run sforge from:
#   .venv/bin/pip install -r oauth-bridge/requirements.txt
fastapi>=0.115
uvicorn>=0.30
httpx>=0.27
pydantic>=2.10
